Card — every content block on a screen sits in one. 24px radius, `--surface` fill, no shadow.

```jsx
<Card>
  <h3>Разметка данных</h3>
  <Button variant="primary" block>Начать разметку</Button>
</Card>
```
