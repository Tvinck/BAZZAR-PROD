Lucide icon wrapper — use for every glyph in Connect Mobile; never hand-draw SVGs.

```jsx
<Icon name="bell" size={24} color="var(--accent)" />
```

Requires `<script src="https://unpkg.com/lucide@0.462.0/dist/umd/lucide.min.js"></script>` loaded before render. Names are kebab-case Lucide names.
