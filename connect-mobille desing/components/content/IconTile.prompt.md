IconTile — rounded-square icon chip used as the leading visual everywhere (service rows, tiles, notifications).

```jsx
<IconTile icon="shield" bg="var(--green)" color="#fff" />
<IconTile icon="newspaper" />
```
